import os
from pathlib import Path

root = Path(__file__).resolve().parent.parent / "zhixing_data" ## 可换成你自己的文件夹的绝对路径。
api_key = "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # 替换为你的 API key，当前仅支持deepseek的API key，其他平台的API key请自行替换并修改相关代码。

